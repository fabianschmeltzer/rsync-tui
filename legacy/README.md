# Legacy Bash implementation

`rsync-srv-gui` is the original Whiptail prototype. It is retained for
historical reference only and receives no feature or security updates.

Use the `rsync-tui` binary documented in the repository root for new
installations.
